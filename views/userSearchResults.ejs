<!DOCTYPE html>
<html lang="en">
<head>    <link rel="stylesheet" href="/styles.css">
    <title><%= title %> ></title>
</head>
<body>
    <%-include("navbar") %>
    <% if(locals.results) { %>

<h3> <%= title %> </h3>
<ul style="list-style-type: none; margin-left: -1.5rem">
 <li><%= results[0].username %>'s posts</li>
 <hr>
    <% results.forEach(result => { %>
        <ul style="list-style-type: none;">
           
            <li><%= result.message %></li>
            <li style="font-size: 0.6rem; margin-left: 2rem;"><em>(<%=result.added%>)</em></li>
        </ul>
    <% }) %>
    </ul>
<%} %>

</body>
</html>
