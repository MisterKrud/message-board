<!DOCTYPE html>
<html lang="en">
<head>
        <link rel="stylesheet" href="/styles.css">
    <title><%= title %> ></title>
</head>
<body>
    <%-include("navbar") %>
    <% if(locals.results) { %>

<h3> <%= title %>: "<%= queryText %>" </h3>
<ul style="list-style-type: none; margin-left: -1.5rem">

    <% results.forEach(result => { %>
        <ul style="list-style-type: none;">
           
            <li><%= result.message %></li>
             <li style="font-weight: bold; font-size: 1rem;">-<%= result.username %> <span style="font-size: .6rem;"> <%= result.added %></span></li>
             <br>
        </ul>
    <% }) %>
    </ul>
<%} %>

</body>
</html>